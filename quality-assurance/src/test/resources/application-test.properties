# Test Configuration
spring.application.name=quality-assurance-service-test
server.port=0

# Kafka Test Configuration
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=quality-assurance-service-test

# Test Database Configuration
spring.datasource.url=jdbc:h2:mem:qualitytestdb
spring.jpa.hibernate.ddl-auto=create-drop

# Logging for tests
logging.level.com.factory.quality=DEBUG
